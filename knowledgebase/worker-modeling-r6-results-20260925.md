# Modeling R6 固定批次结果：2026-09-25

R0–R5 的代码实现已落地，本轮三组 36 个固定建模任务全部结束。**R4 校准与 P4 发布门槛仍未通过**，
不能将完成测试写成验收通过。执行经过及前三批 R4 校准见 [执行记录](worker-modeling-r0-r6-execution.md)。

## 固定分母与在线结果

按用户的 R6 修订：历史通过的硬表面、参考低模、模块各 1 槽；复用、有机、绑定各 3 槽。
每组 12 槽，候选、`cf86fe4` 基线、完整合同单阶段对照各一组；没有删除、补跑或替换失败槽。
候选冻结于 `de4cf60`；后来发现的问题在 `fix/modeling-r6-audit` 修复，没有混入冻结批次。

| 类别 | 候选 PASS / 槽数 | 基线 PASS / 槽数 | 完整合同单阶段 PASS / 槽数 |
| --- | --- | --- | --- |
| 硬表面 | 1/1 | 0/1 | 0/1 |
| 已有资产复用改色 | 1/3 | 2/3 | 3/3 |
| 参考图低模 | 0/1 | 1/1 | 1/1 |
| 模块门框 | 0/1 | 1/1 | 1/1 |
| 有机静态 | 0/3 | 3/3 | 3/3 |
| 简单绑定 | 1/3 | 0/3 | 3/3 |
| 合计 | **3/12** | **7/12** | **11/12** |

候选为 3 PASS、8 FAIL、1 中断；低模中断保留在分母。基线 7 PASS、5 FAIL；单阶段 11 PASS、1 FAIL。
24 槽对照于 2026-09-25 02:05:28 CST 完成。三组终态审计的输入、规格、已验收产物哈希均通过。
单阶段只移除了分阶段作者流程，共用路由、完整合同与验收；不是完整历史 worker 的复现。

21 个在线已验收产物在统一 DCC 与独立视觉检查中全部通过。参考图项按原合同检查轮廓和色区，
未将其表述为经过 IoU 阈值校准。绑定项只覆盖 DCC、实际 GLB 绑定/动作/变形与视觉，不包含 UE skeletal 验收。

## 复用与引擎证据

复用改色的 9 个登记槽全部完成审计，其中 6 个有已验收产物，3 个没有；源与目标文件哈希均保持不变。

| 版本 | 在线通过且实际复用、几何不变、材质改变 |
| --- | --- |
| 候选 | **0/3**：唯一通过槽回退至直接建模，几何改变 |
| 基线 | **2/3** |
| 完整合同单阶段 | **3/3** |

几何比较使用 1 微米量化的世界空间三角形哈希，不依赖对象名称或顶点排列。
三角形数量相同本身不等于几何不变；候选通过槽的源/目标都是 2,156 三角形，仍被正确识别为改变。

候选模块失败槽留存的同一份 FBX 经复制后，补充 DCC 通行检查、UE 导入与独立视觉均通过，
UE 报告为 `ENGINE_READY`，12 项 gate 全部 PASS，原 13 份留存文件哈希不变。
检查包括尺寸、材质、碰撞、LOD、场景放置、实例缩放、保存包及实际 Visibility 简单碰撞胶囊扫掠。
新增通行合同是半径 0.42 m、半高 0.96 m、Z 轴胶囊，中心线从 `[0,-1,0.97]` 到 `[0,1,0.97]`，
margin 0.005 m；对完整 UCX 对齐后的对称映射逐一验证。
这是 `retained-unaccepted` 产物的补充证据，**不提升原始模块任务的 FAIL**。

## 共同检查的纠错

首轮共同检查于 02:18:59 CST 结束，覆盖 36 个固定槽（35 FINISHED、1 INTERRUPTED）。
留存产物清单包含 final 和嵌套 blockout；检查器按排序后的第一个 `source.blend` 选目录，
错误地查找 `blockout/model.fbx`，使 5 个任务的 10 个完整 final 尝试产生错误的 ARTIFACT_GAP。

`2f29fb8` 将 final 所在目录显式登记为 `modelDirectory`；所有嵌套文件仍参与哈希校验。
新增 Windows 回归同时保留完整 blockout 和 final，确认检查 final FBX、有效质量 GAP 不重抽评审。
受影响的候选 modular-1、rig-1 及基线 rig-1/2/3 单独登记于 `common-recheck-correction-v2.json`，
启动前核对原登记的 176 个文件；不启动作者、不更改需求、预算或原在线分母。
原 v1 报告及错误行全部保留，修正报告单独保存；其余 31 个槽没有重复抽取视觉结论。

纠错检查于 2026-09-25 16:28:14 CST 完成：**10 份 final 中 8 PASS、1 TECHNICAL_GAP、1 VISUAL_GAP**，
每份输入文件哈希均保持不变。候选模块及 rig-1、基线 rig-1 的两份、基线 rig-2 的第二份、
基线 rig-3 的三份留存 final 通过；两个真实 GAP 均来自基线 rig-2，详见下表。
36 个任务最终覆盖为：21 个已有接受结果通过共同检查、5 个失败任务具有上述 10 份留存产物、
9 个任务无完整产物、1 个中断。以上是不同层级的分母，不把 8 份留存 PASS 加进在线通过率。

## 失败类型与解释边界

| 类型 | 本轮证据及处理 |
| --- | --- |
| 上游服务/传输 | 候选 503/502 事件为 161/6，基线 503 为 118，单阶段 503 为 49；另有断流事件 3/7/2。CLI 重连事件不是独立宿主调用 |
| Windows 路径 | 候选源预览报告路径 261 字符失败，导致复用回退；后续 extended-path 与短预览目录修复已验证，未替换原槽 |
| 执行记录大小 | 候选 reuse-2、rig-1 内联图片使旧 execution.json 超过 2 MiB；后续 protocol 3 外置大结果并保留哈希/预算 |
| 停止与预算 | 候选 reuse-3 STOP_UNCONFIRMED、低模宿主中断、模块/绑定作者超时；保留原始故障，不能写成视觉不达标 |
| Blender 工具 | API 枚举、旧 Action 属性、MCP 输入或执行错误单独记录；后续 helper 使用说明已更新，但新提示未在本冻结批次验证 |
| 真实技术 GAP | 基线 rig-2 第一次留存 final 的 source 与实际 GLB 都没有合格绑定，Wave 未形成有效顶点变形 |
| 真实视觉 GAP | 基线 rig-2 第三次留存 final 要求单臂挥动，实际两臂同时抬起；首次有效 GAP 即终结本次评审 |
| 检查器缺陷 | 上述 blockout/final 路径混淆单独修复，不计为模型缺件 |

旧基线只有通用 `Modeling quality budget exhausted` 终态，不含完整持久化调用记录，
因此终态保持 `UNCLASSIFIED`，不能仅凭这个文本推断全部为质量失败。
候选/单阶段记录到 72/58 次持久化宿主调用；旧基线只能给出保留日志文件提供的至少 79 次调用。
usage 只覆盖有完成事件的调用，不能据此给出完整 token 成本或金额。

候选先执行、对照后执行；服务状态、路径长度及执行顺序存在混杂。
**11/12 对 3/12 不能证明单阶段架构具有因果性能优势**，本轮只提供诊断线索。

## 验证与当前部署

- Windows worker 单元测试 **142/142 PASS**，无跳过；日志 `r6-final-worker-tests-20260925.log`。
- 本次目录修复相关 8 项测试通过，UE 前置失败/来源校验/哈希变化及不覆盖证据的 3 项测试通过。
- `2a753e1` 提交 UE 留存模型及补充通行入口，并保证前置失败也写 `probe-result.json`，避免下游无限等待。
- 所有代码改动仅在 `worker/`、`skills/`；Markdown 文档按仓库规则豁免。提交前检查暂存路径与 whitespace。
- worker 仍部署 `de4cf60ad5a7f1459763c2e10f6949b98f239967`；本轮后续修复没有部署，也没有冒充已在线回归的候选。
- 2026-09-25 16:21 CST monitor：本地 IDLE、journal 空，controller ONLINE、active=null、queue=0。
- `tripo.txt` 未进入 Git 跟踪且在原工作树被忽略；未输出密钥。固定批次 providerEnabled=false。

R4 三批独立校准分别为 54/60、56/60、37/60 有效槽位，均未满足预登记的 60/60。
维护者控制样本不是人工真值，边界例存在分歧；不合并各批有效槽位或重抽到 PASS。
第三方缺 key、无 credits、不可用结果的回退有确定性 probe；本批没有在线 Tripo 成本或服务质量结论。
后续若验证修复后的候选，应另行冻结版本与清单，先满足 R4 门槛，再按已约定配额执行新的 R6；
保留本批失败、顺序偏差和所有原始证据。

## 证据索引

根目录：`D:\StoneWorker\modeling-v2-audit\r0-r3-20260924`。

- 固定清单：`r6-prepared/batch-manifest-v2.json`。
- 在线终态：`r6-online-comparison-v2.json`、三份 `r6-*-final-audit-v2.json`。
- 完整汇总：`r6-final-summary-v1.json`，包含各来源文件 SHA-256、36 槽逐项诊断和未通过的发布门槛。
- 共同检查：`r6-common-recheck-v1/report.json`；路径纠错：`r6-common-recheck-correction-v2/report.json`。
- 复用差异：`r6-reuse-comparison-v1/report.json`。
- UE：`D:\StoneWorker\ue-r6-modular-v1\run\probe-result.json` 及其引用的 DCC、UE、视觉报告。
- 当前 worker 状态：`worker-allocation-snapshot-20260925-162114.json` 是本次核验，不能回填为昨日部署前证据。
